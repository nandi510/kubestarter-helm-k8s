apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache3.fullname" . }}
  labels:
    {{- include "apache3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache3.selectorLabels" . | nindent 4 }}
